<!-- AZLOCAL-README-VERSION: 1.0.0 -->
<!--
  This README was generated by the AzLocal.UpdateManagement PowerShell module
  (Copy-AzLocalPipelineExample / Update-AzLocalPipelineExample). The module
  refreshes this file IN PLACE when it ships a newer template version, so the
  guidance and links below stay current. To make this README your own and stop
  the module touching it, delete the AZLOCAL-README-VERSION marker line above
  (or replace the file entirely) - a marker-less README is treated as
  operator-owned and is never modified.
-->
# Azure Local Update Management - CI/CD Pipelines

This repository runs **Azure Local (Azure Stack HCI) update management** at fleet
scale, powered by the [`AzLocal.UpdateManagement`](https://aka.ms/AzLocal.UpdateManagement)
PowerShell module. The pipelines assess update readiness, apply updates, and
monitor in-flight update runs across your clusters.

## What's in this repo

- **Pipelines:** `__WORKFLOW_SUBPATH__` (platform: **__PLATFORM__**)
- **Configuration:** `config/`
- **Turnkey refresh script:** `Update-Module-And-Pipelines.ps1` (repo root)

## Refresh after a new module release

When a new `AzLocal.UpdateManagement` version is published, run the turnkey
script from the repo root. It upgrades the module, regenerates the pipelines
(preserving your `BEGIN/END-AZLOCAL-CUSTOMIZE` edits), then commits and pushes -
in one step:

```powershell
.\Update-Module-And-Pipelines.ps1
```

Use `-WhatIf` to preview, or `-NoPush` to refresh without committing.

## Documentation

- **Module overview, cmdlet reference, RBAC:** https://aka.ms/AzLocal.UpdateManagement
- **CI/CD pipeline runbook (setup, triggers, ITSM, troubleshooting):** https://aka.ms/AzLocal.UpdateManagement/CICD

---
<sub>Managed by AzLocal.UpdateManagement. The marker comment at the top of this
file lets the module refresh it when a newer template ships; remove that line to
freeze this README as your own.</sub>
